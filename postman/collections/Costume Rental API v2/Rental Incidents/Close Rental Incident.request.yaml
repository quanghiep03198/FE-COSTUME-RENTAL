$kind: http-request
description: Đóng một sự cố thuê đã được xử lý.
url: "{{baseUrl}}/rental-incidents/:id/close"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: id
    value: "2"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "note": "Đóng sự cố thuê"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "note": {
          "type": "string"
        }
      }
    }
order: 5000
