$kind: http-request
description: Hủy phiếu bảo trì đang mở hoặc đang xử lý.
url: "{{baseUrl}}/maintenance-tickets/:id/cancel"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: id
    value: "2"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "inventory_condition_id": 1,
      "note": "Không cần sửa nữa"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "inventory_condition_id": {
          "type": "integer"
        },
        "note": {
          "type": "string"
        }
      }
    }
order: 6000
