$kind: http-request
description: Thêm một dòng chi tiết vào phiếu mượn nội bộ.
url: "{{baseUrl}}/internal-borrow-slips/:id/details"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: id
    value: "3"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "equipment_prop_id": 19,
      "borrowed_quantity": 1,
      "remarks": "Đạo cụ cần mượn"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "equipment_prop_id": {
          "type": "integer"
        },
        "borrowed_quantity": {
          "type": "integer"
        },
        "remarks": {
          "type": "string"
        }
      }
    }
order: 5000
